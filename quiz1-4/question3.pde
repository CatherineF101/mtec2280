//question3
void setup(){
  size(500, 500);

}

void draw (){
    background(255);
    line(0, 0, mouseX, mouseY); 
    line(0, height, mouseX, mouseY);
    line(width, 0, mouseX, mouseY);
    line(width, height, mouseX, mouseY);
    
    //positions of lines on upper right left and lower right left corners w/ mouse drag
    
}